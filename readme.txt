A simple calculator app made using android studio. Kotlin is the main language used in it. Only interface and 
interaction with buttons are handled, expression will not actually be handled and evaluatd will be written in output.
I have only pushed files in whish I worked, rest are default.